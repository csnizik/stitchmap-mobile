import { View } from 'react-native';

import SkiaHost from '../../components/SkiaHost';

export default function Home() {
  return (
    <View className="flex-1 items-center justify-center">
      <SkiaHost />
    </View>
  );
}
